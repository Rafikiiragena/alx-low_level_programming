#include "function_pointers.h"

/**
 * int_index - function that searches for an integer
 * @array: array
 * @size: size
 * @cmp: point to a function that takes int as a parametre return int
 *
 * Return: int
 */
int int_index(int *array, int size, int (*cmp)(int))
{
	return (0);
}
