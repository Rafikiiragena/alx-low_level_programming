#include "calc.h"

/**
 * get_op_func - function that selects the correct function
 * to perform th operation asked by the user
 * @s: s is the operator passed as argument to the program
 *
 * Return: int
 */
int (*get_op_func(char *s))(int, int)
{
	return (0);
}
