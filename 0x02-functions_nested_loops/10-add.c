#include "main.h"
#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result
 * @i: print int i
 * @k: print int k
 * Return: Always 0.
 */
int add(int i, int k)
{
	return (i + k);
}
